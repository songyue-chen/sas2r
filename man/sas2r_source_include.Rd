% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/runtime-include.R
\name{sas2r_source_include}
\alias{sas2r_source_include}
\title{Run an included module where the \%INCLUDE stood}
\usage{
sas2r_source_include(relative_path, envir = parent.frame())
}
\arguments{
\item{relative_path}{The module's path relative to the bundle root.}

\item{envir}{The environment to run it in; the caller's by default.}
}
\value{
The resolved path of the module, invisibly.
}
\description{
Each included SAS file is translated once into its own staged module; this
executes that module at one include site, into the calling program's own
environment, exactly where the \verb{\%INCLUDE} stood.
}
\details{
The path is always relative to the staged bundle root -- the nearest
ancestor of the running script holding \verb{_sas2r_registry.R} -- and never to
the working directory, so the bundle can be run from anywhere. Absolute
paths and \code{.} or \code{..} components are rejected, and the file the joined path
actually resolves to is then required to lie under the root: rejecting the
components alone would still let a symlinked directory inside the bundle
read outside it, so the confinement is checked on the resolved path, where
a hand-edited bundle cannot talk its way out.
}
\examples{
\dontrun{
# inside a translated program, where the SAS source had \%include 'inc/prep.sas';
sas2r_source_include("inc/prep.R")
}
}
\seealso{
Other runtime helpers: 
\code{\link{apply_format}()},
\code{\link{chr_cmp}()},
\code{\link{lib_read}()},
\code{\link{sas2r_fold_names}()},
\code{\link{sas2r_libname_assign}()},
\code{\link{sas_compress}()},
\code{\link{sas_display}()},
\code{\link{sas_if_else}()},
\code{\link{sas_merge}()},
\code{\link{sas_min}()},
\code{\link{sas_round}()},
\code{\link{sas_sort}()},
\code{\link{sas_sum}()}
}
\concept{runtime helpers}
