% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc-profile.R
\name{qc_profile}
\alias{qc_profile}
\title{Define reusable dataset quality requirements}
\usage{
qc_profile(
  required_columns = NULL,
  labels = NULL,
  formats = NULL,
  types = NULL,
  column_order = NULL,
  keys = NULL,
  unique_keys = FALSE,
  row_count = NULL,
  min_rows = NULL,
  max_rows = NULL,
  numeric_tolerance = NULL,
  tolerances = list()
)
}
\arguments{
\item{required_columns}{Character vector of required column names.}

\item{labels, formats}{Named character vectors of exact required variable
labels and \code{format.sas} attributes. Missing attributes fail the check.}

\item{types}{Named character vector: \code{numeric} (integer or double),
\code{double}, \code{integer}, \code{character}, \code{logical}, \code{Date}, or \code{POSIXct}.}

\item{column_order}{Exact ordered vector of all expected column names.}

\item{keys}{Row alignment columns, matched case-insensitively. Requiring
uniqueness also requires nonmissing, nonblank key values.}

\item{unique_keys}{Whether the combined key must be nonmissing and unique.}

\item{row_count, min_rows, max_rows}{Exact, minimum, or maximum row count.}

\item{numeric_tolerance}{Absolute tolerance for reference comparison. When
set, the relative tolerance defaults to zero, as for target assertions.}

\item{tolerances}{Named variable overrides with \code{abs} and/or \code{rel}, using
the same policy as \code{\link[=compare_profile]{compare_profile()}}. Only evaluated with a reference.}
}
\value{
A plain list containing only explicitly supplied, non-NULL fields.
Omitted fields inherit global settings. Explicit \code{FALSE} and empty lists
override them. Types describe physical R columns: factors do not satisfy
\code{character}, even when their values compare equal to character data.
}
\description{
Assign a profile a name in \code{outputs$profiles}, then select it with
\code{outputs$assertions[["adam.adsl"]]$profile}. Target assertions override
whole profile fields. These are explicit study requirements, not an
automatic CDISC compliance assessment or evidence of SAS equivalence.
}
\examples{
adsl_qc <- qc_profile(
  required_columns = c("USUBJID", "AGE"),
  keys = "USUBJID", unique_keys = TRUE,
  types = c(USUBJID = "character", AGE = "numeric"),
  min_rows = 1, tolerances = list(AGE = list(abs = 0, rel = 0))
)
}
