% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/llm.R
\name{sas_llm}
\alias{sas_llm}
\title{Construct an LLM adapter from a project's \verb{llm:} configuration}
\usage{
sas_llm(config)
}
\arguments{
\item{config}{An \verb{llm:} mapping, a \code{sas2r_config} from \code{\link[=sas_config]{sas_config()}}, or an
already-normalized LLM list.}
}
\value{
A \code{sas2r_llm} adapter.
}
\description{
The supported way to obtain the \code{llm} argument that \code{\link[=sas_translate]{sas_translate()}} accepts.
The adapter is backed by public \code{ellmer} APIs for one of the twelve registered provider
ids; credentials stay with \code{ellmer} and are never read or stored by sas2r.
Constructing an adapter contacts no network -- use \code{\link[=sas_llm_probe]{sas_llm_probe()}} to
validate connectivity and model capabilities, and \code{\link[=sas_llm_models]{sas_llm_models()}} to see
the inventory visible to the configured identity.
}
\examples{
# Constructing an adapter contacts no network and reads no credentials.
if (requireNamespace("ellmer", quietly = TRUE)) {
  llm <- sas_llm(list(provider = "anthropic", model = "claude-sonnet-4-6"))
  class(llm)
}

\dontrun{
# Any ellmer-supported provider works; credentials stay with ellmer.
llm <- sas_llm(list(provider = "openai", model = "gpt-4.1"))
res <- sas_translate("path/to/sas", llm = llm)
}
}
\seealso{
\code{\link[=sas_llm_probe]{sas_llm_probe()}}, \code{\link[=sas_llm_models]{sas_llm_models()}}
}
