% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preflight.R
\name{sas_preflight}
\alias{sas_preflight}
\title{Inspect a migration and optionally diagnose findings}
\usage{
sas_preflight(
  path,
  out_dir = NULL,
  config = NULL,
  outputs = NULL,
  budget_usd = Inf,
  budget_mode = "stop",
  pricing_source = "catalog",
  pricing_rates = NULL,
  usage_limits = NULL,
  recursive = FALSE,
  max_parallel_translations = NULL,
  diagnose = c("auto", "off"),
  llm = NULL
)
}
\arguments{
\item{path}{Path to a SAS file or directory containing SAS files, or a \code{sas2r_project}.}

\item{out_dir}{Planned migration output root. \code{NULL} reports a temporary
output root, as used by \code{sas_translate()}, without creating it.}

\item{config}{Optional configuration list, YAML path, or \code{sas2r_config} object.
With a reused project, a plain list replaces only its supplied top-level
fields; omitted fields inherit, and explicit \code{NULL} resets a field. A YAML
path or \code{sas2r_config} object supplies a complete configuration, as does a
plain list with a source path. Changing library or source-search settings
requires rescanning the source path. Reference paths in YAML resolve from
its directory; paths in R lists resolve from the project directory.}

\item{outputs}{Optional character vector or list specifying output contract overrides.
Reference paths supplied here resolve from the calling working directory.
Output overrides are retained in the returned project for reuse.}

\item{budget_usd}{Dollar threshold for LLM spend. Defaults to Inf (unlimited).}

\item{budget_mode}{Dollar enforcement mode: "stop", "observe", "soft", or "strict". Defaults to "stop".}

\item{pricing_source}{Cost provenance source ("catalog", "adapter", "organization", "external"). Defaults to "catalog".}

\item{pricing_rates}{Optional organization pricing table.}

\item{usage_limits}{Optional named list of non-dollar request limits:
\code{max_calls}, \code{max_retries}, \code{max_tool_calls}, \code{max_wall_time} (seconds),
\code{max_request_bytes}, \code{max_request_chars}, \code{max_input_tokens}, and
\code{max_output_tokens}. Each defaults to Inf. Zero prevents the corresponding
request activity; unknown names and invalid values raise a configuration error.}

\item{recursive}{Logical; whether to scan subdirectories recursively. Defaults to FALSE.}

\item{max_parallel_translations}{Concurrent component workflow limit used
when planning translation. Overrides \code{migration.max_parallel_translations}.
Invalid combinations are reported by static checks; automatic diagnosis
can explain these errors when a valid diagnosis budget is available.}

\item{diagnose}{\code{"auto"} (default) diagnoses actionable findings when an LLM
is configured; \code{"off"} always stays offline. Advice never clears findings.}

\item{llm}{Optional \code{sas2r_llm} adapter for diagnosis, overriding \verb{llm:}
configuration. Missing configuration is reported, not treated as a failure.}
}
\value{
A \code{sas2r_preflight} list with \code{sources}, point-of-use \code{libraries},
\code{inputs} (available, missing, unresolved, no_producer, deferred, backward_dependency,
created_if_missing, generated, or environment), \code{references}, \code{unsupported},
scanner \code{findings}, shared \code{readiness} warnings, \code{outputs}, \code{destinations}, \code{budget}, \code{next_actions}, \code{diagnosis}, and actual \code{model_calls}.
Diagnosis failures preserve the static result. Inspection errors retain
their original condition with optional advice attached as \verb{$diagnosis}.
\code{called_macros} lists reachable search-path macro definitions and their
standalone R and interface-test paths.
\code{pipeline} reconciles every scanned file with the translation schedule and
bundle execution roles, listing intentional exclusions and blocking gaps.
Also includes \code{status}, \code{configured_libraries}, \code{schedule}, explanatory \code{notes},
\code{max_parallel_translations}, and the scanned \code{project}, reusable by \code{sas_translate()} while sources are unchanged.
\code{ready_for_translation} means no known missing inputs or unresolved scan
findings; it does not mean execution or reference validation has passed.
}
\description{
Scans sources and resolves library bindings with the same project scanner
used by \code{\link[=sas_translate]{sas_translate()}}. Reads directory listings, not dataset contents.
Does not execute programs, read dataset contents, or create output directories,
scan caches, or usage ledgers. With a configured LLM, actionable findings can
receive one bounded advisory diagnosis. Use \code{diagnose = "off"} for offline
inspection without constructing an adapter or making a model call.
Deterministic support findings are advisory: an AI translation may handle
deferred constructs, and runtime checks can still defer supported shapes.
}
\examples{
source <- tempfile(fileext = ".sas")
writeLines("data out; set raw.dm; run;", source)
check <- sas_preflight(source, usage_limits = list(max_calls = 20))
check$inputs
check$model_calls
unlink(source)
}
