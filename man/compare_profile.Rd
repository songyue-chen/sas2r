% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/profile.R
\name{compare_profile}
\alias{compare_profile}
\title{Build a comparison tolerance profile}
\usage{
compare_profile(
  abs = 1e-08,
  rel = 1e-08,
  sas_null_equals_na = TRUE,
  na_tags = c("report", "ignore", "strict"),
  padding = c("cosmetic", "strict"),
  attrs_cosmetic = c("label", "format.sas"),
  overrides = list(),
  keys = NULL
)
}
\arguments{
\item{abs}{Absolute numeric tolerance (scalar >= 0).}

\item{rel}{Relative numeric tolerance (scalar >= 0).}

\item{sas_null_equals_na}{Whether SAS missing character values (empty string \code{""} and all-space strings) equal R NA (logical).}

\item{na_tags}{Tagged-NA handling: \code{"report"}, \code{"ignore"}, or \code{"strict"}.}

\item{padding}{Trailing blank padding handling: \code{"cosmetic"} or \code{"strict"}.}

\item{attrs_cosmetic}{Character vector of attribute names whose differences are cosmetic.}

\item{overrides}{Named list of per-variable numeric tolerance overrides.}

\item{keys}{Character vector of key column names, or \code{NULL}.}
}
\value{
An object of class \code{sas2r_profile}.
}
\description{
Every equivalence judgement the comparator makes is parameterized here
and nowhere else. Defaults implement the accepted SAS-to-R tolerance
rules: SAS null == NA, special missings reported, trailing padding
cosmetic, combined absolute + relative numeric tolerance.
}
\examples{
# Defaults implement the accepted SAS-to-R tolerance rules.
p <- compare_profile()
p$numeric

# Tighten numeric tolerance and make trailing blanks significant.
strict <- compare_profile(abs = 1e-12, rel = 1e-12, padding = "strict")

# Per-variable tolerance override, plus keys used for row matching.
compare_profile(overrides = list(aval = list(abs = 1e-6)),
                keys = c("usubjid", "paramcd"))
}
