% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/runtime-data.R
\name{lib_delete}
\alias{lib_delete}
\title{Delete explicitly named datasets from a writable library}
\usage{
lib_delete(libref, members)
}
\arguments{
\item{libref}{A configured library name, such as \code{"work"}.}

\item{members}{Character vector of explicit dataset names, without librefs
or file extensions. An empty vector does nothing.}
}
\value{
The names of removed members, invisibly.
}
\description{
Removes the stored dataset, rather than just an R object. Missing members
produce a message and do not stop deletion of other members. Names are
matched case-insensitively across rds, xpt and sas7bdat files.
}
\details{
Only an explicit vector of ordinary SAS member names is supported. Expand
a known list into names before calling; prefix lists, ranges and \verb{_ALL_}
are not interpreted. Dynamic selection that cannot be resolved must remain
an explicit unsupported operation, never a silent no-op.

This helper deletes from the configured write directory. If a requested
member also exists in a different read directory, deletion is unsupported:
removing the written copy would expose that original again to \code{\link[=lib_read]{lib_read()}}.
The whole request is checked before any file is removed, and input library
files in a separate read directory are never deleted.
}
\examples{
.sas2r_registry <- list(work = list(path = tempfile("work"), write = "rds"))
lib_write(data.frame(x = 1), "work", "scratch")
lib_delete("work", "scratch")
rm(.sas2r_registry)
}
\seealso{
Other runtime helpers:
\code{\link{apply_format}()},
\code{\link{chr_cmp}()},
\code{\link{lib_exists}()},
\code{\link{lib_members}()},
\code{\link{lib_read}()},
\code{\link{sas2r_fold_names}()},
\code{\link{sas2r_libname_assign}()},
\code{\link{sas2r_resolve_registry}()},
\code{\link{sas2r_source_include}()},
\code{\link{sas_compress}()},
\code{\link{sas_display}()},
\code{\link{sas_if_else}()},
\code{\link{sas_merge}()},
\code{\link{sas_min}()},
\code{\link{sas_missing}()},
\code{\link{sas_round}()},
\code{\link{sas_sort}()},
\code{\link{sas_sum}()}
}
\concept{runtime helpers}
