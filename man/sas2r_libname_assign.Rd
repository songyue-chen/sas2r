% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/runtime-data.R
\name{sas2r_libname_assign}
\alias{sas2r_libname_assign}
\alias{sas2r_libname_clear}
\title{Bind or clear a libref at its point of use}
\usage{
sas2r_libname_assign(
  libref,
  read_path,
  write_path = read_path,
  engine = "sas7bdat",
  write = "rds"
)

sas2r_libname_clear(libref)
}
\arguments{
\item{libref}{The libref (case-insensitive).}

\item{read_path, write_path}{Directories the libref reads from and writes to.}

\item{engine}{Format of members read: \code{"sas7bdat"}, \code{"xpt"}, or \code{"rds"}.}

\item{write}{Format members are written in: \code{"rds"} or \code{"xpt"}.}
}
\value{
\code{sas2r_libname_assign()} returns the new registry entry, invisibly;
\code{sas2r_libname_clear()} returns \code{NULL}, invisibly.
}
\description{
The registry a bundle starts with is only a seed -- the libraries the
configuration declares, plus \code{work}. Every \code{LIBNAME} the SAS program
executes changes the registry where that statement stood, so a libref that
is rebound or cleared part-way through behaves at run time the way it does
in SAS. \code{sas2r_libname_assign()} is \verb{LIBNAME libref '<path>'};
\code{sas2r_libname_clear()} is \verb{LIBNAME libref CLEAR}.
}
\examples{
.sas2r_registry <- list()
sas2r_libname_assign("raw", tempdir(), engine = "rds")
names(.sas2r_registry)
sas2r_libname_clear("raw")
names(.sas2r_registry)
rm(.sas2r_registry)
}
\seealso{
Other runtime helpers: 
\code{\link{apply_format}()},
\code{\link{chr_cmp}()},
\code{\link{lib_read}()},
\code{\link{sas2r_fold_names}()},
\code{\link{sas2r_source_include}()},
\code{\link{sas_compress}()},
\code{\link{sas_display}()},
\code{\link{sas_if_else}()},
\code{\link{sas_merge}()},
\code{\link{sas_min}()},
\code{\link{sas_round}()},
\code{\link{sas_sort}()},
\code{\link{sas_sum}()}
}
\concept{runtime helpers}
