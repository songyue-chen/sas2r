% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/runtime-data.R
\name{sas2r_resolve_registry}
\alias{sas2r_resolve_registry}
\title{Resolve a bundle's libref registry against its folder}
\usage{
sas2r_resolve_registry(registry, root)
}
\arguments{
\item{registry}{The \code{.sas2r_registry} list as written in \code{autoexec.R}.}

\item{root}{The bundle folder, as an absolute path.}
}
\value{
The registry, with every \code{read_path} and \code{write_path} absolute.
}
\description{
\code{autoexec.R} lists each libref's directories the way a person writes them:
an absolute path is used as is, and a relative path means inside the bundle
folder. This turns the relative ones into absolute paths against \code{root},
creates every write directory, and returns the registry ready for
\code{\link[=lib_read]{lib_read()}} and \code{\link[=lib_write]{lib_write()}}. \code{autoexec.R} calls it once, after the
helpers are loaded; call it yourself only when building a registry by hand.
}
\details{
A \verb{<FILL>} placeholder left in a path -- the commented entries \code{autoexec.R}
carries for a library nothing binds -- is refused with the libref named,
rather than becoming a directory called \verb{<FILL...>}.
}
\examples{
root <- tempfile("bundle"); dir.create(root)
reg <- sas2r_resolve_registry(
  list(work = list(read_path = "work", write_path = "work",
                   engine = "rds", write = "rds")),
  root)
reg$work$write_path      # <root>/work, and the directory now exists
}
\seealso{
Other runtime helpers: 
\code{\link{apply_format}()},
\code{\link{chr_cmp}()},
\code{\link{lib_read}()},
\code{\link{sas2r_fold_names}()},
\code{\link{sas2r_libname_assign}()},
\code{\link{sas2r_source_include}()},
\code{\link{sas_compress}()},
\code{\link{sas_display}()},
\code{\link{sas_if_else}()},
\code{\link{sas_merge}()},
\code{\link{sas_min}()},
\code{\link{sas_round}()},
\code{\link{sas_sort}()},
\code{\link{sas_sum}()}
}
\concept{runtime helpers}
