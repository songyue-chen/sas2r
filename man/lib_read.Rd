% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/runtime-data.R
\name{lib_read}
\alias{lib_read}
\alias{lib_write}
\title{Read and write datasets through librefs}
\usage{
lib_read(libref, member, ...)

lib_write(df, libref, member, ...)
}
\arguments{
\item{libref}{The libref, as a single string.}

\item{member}{The dataset member name, as a single string.}

\item{...}{Not used; present so a rejected alias fails with a clear message.}

\item{df}{The data frame to write.}
}
\value{
\code{lib_read()} returns the dataset with class \code{sas2r_dataset};
\code{lib_write()} returns \code{df}, invisibly.
}
\description{
\code{lib_read("lib", "member")} reads a dataset from a libref as the registry
configures it: the libref's write path is tried first (so a dataset the
bundle produced earlier wins), then its read path, for \code{.rds},
\code{.sas7bdat}, and \code{.xpt} in that order. The result carries class
\code{sas2r_dataset}, whose \code{$} and \code{[[} methods resolve column names
case-insensitively, as SAS variable names do. \code{lib_write(df, "lib", "member")} writes to the libref's write path in its configured format
(\code{rds} or \code{xpt}), creating the directory on demand.
}
\details{
Exactly one call form is accepted for each. Combined \code{"lib.member"}
strings, single-argument calls, and \verb{dataset=}/\verb{table=} aliases fail with
the canonical form in the message, so a wrong call in an agent translation
becomes repair feedback instead of being silently reinterpreted.

The registry is the \code{.sas2r_registry} list a bundle's \code{autoexec.R}
defines; in a translated program it is loaded by the bootstrap header. To
use these helpers interactively, source that file first, with
\code{chdir = TRUE}.
}
\examples{
lib <- tempfile("lib"); dir.create(lib)
.sas2r_registry <- list(
  raw = list(read_path = lib, write_path = lib, engine = "rds", write = "rds")
)
lib_write(data.frame(USUBJID = c("01", "02"), AVAL = c(1, 2)), "raw", "dm")
dm <- lib_read("raw", "dm")
dm$usubjid          # case-insensitive column access
rm(.sas2r_registry)
}
\seealso{
Other runtime helpers: 
\code{\link{apply_format}()},
\code{\link{chr_cmp}()},
\code{\link{sas2r_fold_names}()},
\code{\link{sas2r_libname_assign}()},
\code{\link{sas2r_resolve_registry}()},
\code{\link{sas2r_source_include}()},
\code{\link{sas_compress}()},
\code{\link{sas_display}()},
\code{\link{sas_if_else}()},
\code{\link{sas_merge}()},
\code{\link{sas_min}()},
\code{\link{sas_round}()},
\code{\link{sas_sort}()},
\code{\link{sas_sum}()}
}
\concept{runtime helpers}
