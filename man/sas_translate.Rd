% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/translate.R
\name{sas_translate}
\alias{sas_translate}
\title{Translate SAS programs or project to R with dependency-aware orchestration}
\usage{
sas_translate(
  path,
  out_dir = NULL,
  config = NULL,
  execute = TRUE,
  max_program_repair_rounds = 1L,
  max_bundle_repair_rounds = NULL,
  outputs = NULL,
  agent_evidence = c("code_only", "bounded"),
  llm = NULL,
  budget_usd = Inf,
  budget_mode = "stop",
  pricing_source = "catalog",
  pricing_rates = NULL,
  usage_limits = NULL,
  recursive = FALSE,
  resume = FALSE,
  keep_raw_attempts = FALSE,
  max_bundle_repairs_per_component = 2L,
  max_parallel_translations = NULL
)
}
\arguments{
\item{path}{Path to a SAS file or directory containing SAS files, or a \code{sas2r_project}.}

\item{out_dir}{Output directory path for generated R bundle, attempts, and reports. Defaults to a temporary directory.
Relative paths resolve from the calling working directory before workers start.}

\item{config}{Optional configuration list, YAML path, or \code{sas2r_config} object.
With a reused project, a plain list replaces only its supplied top-level
fields; omitted fields inherit, and explicit \code{NULL} resets a field. A YAML
path or \code{sas2r_config} object supplies a complete configuration, as does a
plain list with a source path. Changing library or source-search settings
requires rescanning the source path. Reference paths in YAML resolve from
its directory; paths in R lists resolve from the project directory.}

\item{execute}{Logical; whether to run meaningful program smoke and full bundle execution. Defaults to TRUE.}

\item{max_program_repair_rounds}{Maximum repair rounds per component in immediate loop. Defaults to 1L.}

\item{max_bundle_repair_rounds}{Optional overall cap on bundle fixer calls.
Defaults to NULL, allowing each component its own bounded repair allowance.
An explicit zero disables bundle repair.}

\item{outputs}{Optional character vector or list specifying output contract overrides.
Reference paths supplied here resolve from the calling working directory.
Output overrides are retained in the returned project for reuse.}

\item{agent_evidence}{Agent evidence policy ("code_only" or "bounded"). Defaults to "code_only".}

\item{llm}{Optional \code{sas2r_llm} instance for agent-assisted translation, review, and repair.}

\item{budget_usd}{Dollar threshold for LLM spend. Defaults to Inf (unlimited).}

\item{budget_mode}{Dollar enforcement mode: "stop", "observe", "soft", or "strict". Defaults to "stop".}

\item{pricing_source}{Cost provenance source ("catalog", "adapter", "organization", "external"). Defaults to "catalog".}

\item{pricing_rates}{Optional organization pricing table.}

\item{usage_limits}{Optional named list of non-dollar request limits:
\code{max_calls}, \code{max_retries}, \code{max_tool_calls}, \code{max_wall_time} (seconds),
\code{max_request_bytes}, \code{max_request_chars}, \code{max_input_tokens}, and
\code{max_output_tokens}. Each defaults to Inf. Zero prevents the corresponding
request activity; unknown names and invalid values raise a configuration error.}

\item{recursive}{Logical; whether to scan subdirectories recursively. Defaults to FALSE.}

\item{resume}{Logical; reuse saved translation revisions and completed reviews
when source, input data, configuration, helpers, and worker prompts still
match. Full-bundle execution and output checks use fresh attempts; passing
or deferred smoke results can be reused when their full context matches.
Changing only the parallel translation limit does not invalidate reuse.
Missing or edited revision files trigger regeneration. Compatible version-8
checkpoints are imported; other incompatible checkpoints regenerate with an
explanation before new provider calls. Defaults to FALSE.}

\item{keep_raw_attempts}{Logical; retain raw outputs from unselected attempts,
including isolated component smoke outputs and their replay scripts. These
are partial debugging artifacts, not validated final outputs. Defaults to FALSE.}

\item{max_bundle_repairs_per_component}{Maximum bundle fixer calls per component
across the run. Defaults to 2L; separate from immediate program repairs.}

\item{max_parallel_translations}{Maximum concurrent SAS program or macro translation workflows. Overrides
\code{migration.max_parallel_translations} in \verb{_sas2r.yml}; defaults to 1. Model waiting can
overlap on fewer CPUs. Local execution and repair remain serial.
Values above 1 cannot be combined with \code{llm.max_tries} above 1; preflight
and translation stop with a configuration error before provider calls.}
}
\value{
An object of S3 class \code{"sas2r_translation"} containing \verb{$run_id}, \verb{$out_dir},
\verb{$bundle_dir}, \verb{$outputs_dir}, \verb{$status}, \verb{$status_reason}, \verb{$graph_path},
\verb{$output_contracts_path}, \verb{$report_path}, \verb{$report_json_path}, \verb{$component_evidence},
\verb{$output_assessments}, \verb{$diagnostics}, \verb{$repair_history}, \verb{$usage}, and \verb{$project}.
\verb{$outputs_dir} contains all selected generated files with their library and
relative-path layout (for example \code{work/out.rds}, \code{adam/adsl.rds},
\code{outputs/table.html}); it is NULL when execution is disabled. The JSON
report includes separate target/reference/review coverage and effective limits.
}
\description{
Exposes the single authoritative SAS-to-R migration pipeline. SAS source is
the only required input. Operates on a single SAS file (wrapped as a one-module
project) or a directory of SAS files with identical internal sequencing:
initializes/reconciles run -> discovers dependency graph and output contracts ->
baseline generation and immediate program repair -> full bundle attempt execution
and output-driven repair -> deterministic gate selection and reporting.
}
\details{
Missing inputs, called macros, includes and uncertain dependencies are reported
while available source continues translating. Unavailable dependencies defer
affected execution; they do not authorize inventing missing source or data.
Component failures preserve useful work and allow other components to proceed.
Unusable configuration, inconsistent pipeline coverage and critical provider or
accounting failures still stop the run. Use \code{\link[=sas_preflight]{sas_preflight()}} to inspect
readiness and configure \code{macros.search_path} for external macro definitions.
}
\examples{
# Translate a small SAS program with the deterministic rule-based engine.
# Neither an LLM nor a SAS installation is required.
sas_dir <- file.path(tempdir(), "sas2r-translate-example")
dir.create(sas_dir, showWarnings = FALSE)
writeLines(c(
  "data work.adsl;",
  "  set adam.dm;",
  "  where age >= 18;",
  "  bmi = weight / (height * height);",
  "run;",
  "",
  "proc sort data=work.adsl out=work.adsl_sorted;",
  "  by usubjid;",
  "run;"
), file.path(sas_dir, "adsl.sas"))

# execute = FALSE translates without running the generated bundle.
res <- sas_translate(sas_dir, out_dir = tempfile("sas2r-out"), execute = FALSE)
res$status
cat(sas_code(res))

\dontrun{
# Run the generated bundle and let the agents translate, review, and repair
# the patterns the deterministic rules cannot prove.
llm <- sas_llm(list(provider = "anthropic", model = "claude-sonnet-4-6"))
res <- sas_translate("path/to/sas/project", llm = llm, recursive = TRUE)
sas_write(res, "path/to/r/bundle")
}
}
