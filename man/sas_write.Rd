% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/translate.R
\name{sas_write}
\alias{sas_write}
\title{Write translated code artifacts and outputs to a destination directory}
\usage{
sas_write(x, dir)
}
\arguments{
\item{x}{A \code{sas2r_translation} object.}

\item{dir}{Target directory path to write translated files.}
}
\value{
The target directory path, invisibly.
}
\description{
Copies the selected programs, runtime, all generated files, and migration
reports. Rebuilds \code{autoexec.R} with destination-relative output paths and
includes a dependency-ordered \code{run.R}, an output manifest, and a README.
Input libraries remain external dependencies documented in the README.
Run \verb{Rscript run.R} from the exported folder, or \code{source("run.R", chdir = TRUE)}.
Warns if status is \code{blocked} or \code{needs_review}.
}
\examples{
sas_dir <- file.path(tempdir(), "sas2r-write-example")
dir.create(sas_dir, showWarnings = FALSE)
writeLines(c("proc sort data=work.dm out=work.dm_sorted;",
             "  by usubjid;",
             "run;"), file.path(sas_dir, "sort.sas"))
res <- sas_translate(sas_dir, out_dir = tempfile("sas2r-out"), execute = FALSE)

# Writing an unverified translation warns; the bundle is still written.
dest <- tempfile("sas2r-bundle")
suppressWarnings(sas_write(res, dest))
list.files(dest, recursive = TRUE)
}
