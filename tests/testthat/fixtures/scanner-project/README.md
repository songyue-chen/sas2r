# Scanner diagnostic fixture

This is deliberately incomplete test material, not a runnable migration tutorial.
`adam.adsl` is absent and `02_summary.sas` calls an unresolved `ghost_macro`.
Tests check source scanning, macro resolution, lineage, and visible translation
stubs. They supply synthetic input when testing the supported `03_t1.sas` path.

For the installed user example, use `examples/migration-demo` in the package.
